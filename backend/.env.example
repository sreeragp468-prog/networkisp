# Server Configuration
PORT=5000

# MongoDB Connection String (Atlas Cloud Database or Local MongoDB)
# Set MONGO_URI in your deployment platform (Vercel / Render / Heroku) or local .env
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/network_isp?retryWrites=true&w=majority
